<div class = "section animated fadeIn">
  <% @people.each do |person| %>
    <div><%= link_to(image_tag(person.image_url, height: '200', width: '200'), person_path(person)) %></div>
  <% end %>
</div>
<%= link_to("Highlighted Stories", '/people', method: :get) %>
